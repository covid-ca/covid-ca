import styled from 'styled-components';
import { graphic } from '../constants/color';

export const DataLabel = styled('p')`
  margin: 0;
  min-width: 50px;
  text-align: end;
  color: ${graphic};
  font-size: 20px;
`;
